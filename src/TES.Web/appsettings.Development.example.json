{
  // ÖRNEK dosyadır — gerçek değerler depoya YAZILMAZ (CLAUDE.md Bölüm 7).
  // Gerçek değerleri user-secrets ile tanımlayın:
  //   dotnet user-secrets set "ConnectionStrings:Default" "Server=.;Database=TES;Trusted_Connection=True;TrustServerCertificate=True" --project src/TES.Web
  "ConnectionStrings": {
    "Default": "Server=.;Database=TES;Trusted_Connection=True;TrustServerCertificate=True"
  }
}
